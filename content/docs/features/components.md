# Components

GoStarter uses templ-based UI components and a showcase-driven workflow.

## Explore components

- `/docs/components` for quick usage references
- `/docs/components/{name}` for focused examples
- `/showcase` for broader visual inspection

## Practical guidance

Prefer shared component classes (`.btn-*`, `.card`, `.input`, `.label`) to keep styling and dark-mode behavior consistent.
